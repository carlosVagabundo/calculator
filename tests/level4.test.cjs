const assert=require("node:assert/strict");const fs=require("node:fs");const path=require("node:path");const e=require("../engine");const{createStorage,DEFAULT_SETTINGS,KEYS}=require("../storage");const root=path.join(__dirname,"..");const html=fs.readFileSync(path.join(root,"index.html"),"utf8");const script=fs.readFileSync(path.join(root,"script.js"),"utf8");const engineSrc=fs.readFileSync(path.join(root,"engine.js"),"utf8");const css=fs.readFileSync(path.join(root,"style.css"),"utf8");const n=(a,b)=>assert.ok(Math.abs(a-b)<1e-10,a+" !== "+b);
n(e.evaluate("asin(0.5)",{angleMode:"DEG"}),30);n(e.evaluate("acos(0.5)",{angleMode:"DEG"}),60);n(e.evaluate("atan(1)",{angleMode:"DEG"}),45);n(e.evaluate("exp(1)"),Math.E);n(e.evaluate("abs(-7)"),7);n(e.evaluate("floor(3.9)"),3);n(e.evaluate("ceil(3.1)"),4);n(e.evaluate("ans+5",{ans:7}),12);n(e.evaluate("2ans",{ans:3}),6);assert.throws(()=>e.evaluate("asin(2)"),/entre -1 e 1/);assert.doesNotMatch(engineSrc,/eval\s*\(/);assert.match(html,/manifest\.webmanifest/);assert.match(html,/data-action="ans"/);assert.match(html,/export-data/);assert.match(html,/import-data/);assert.match(html,/help-dialog/);assert.match(html,/record-search/);assert.match(script,/ans:Number\(state\.ans\)\|\|0/);assert.match(script,/navigator\.serviceWorker\.register/);assert.match(css,/body\[data-theme="light"\]/);
class S{constructor(){this.m=new Map}getItem(k){return this.m.has(k)?this.m.get(k):null}setItem(k,v){this.m.set(k,String(v))}removeItem(k){this.m.delete(k)}}const s=new S;s.setItem("calculator.history.v4",JSON.stringify([{id:"old",expression:"2+2",result:"4"}]));const st=createStorage(s);assert.equal(st.loadHistory()[0].result,"4");assert.equal(st.loadHistory()[0].angleMode,"DEG");assert.deepEqual(st.loadSettings(),DEFAULT_SETTINGS);st.saveHistory([{id:"x",expression:"sqrt(9)",result:"3",timestamp:1}]);st.saveFavorites([{id:"y",expression:"2+2",result:"4"}]);assert.equal(st.snapshot().history.length,1);assert.equal(st.snapshot().favorites.length,1);st.importSnapshot({history:[{expression:"3*3",result:"9"}],favorites:[],settings:{theme:"light",animations:false}});assert.equal(st.loadHistory()[0].expression,"3*3");assert.equal(st.loadSettings().theme,"light");assert.equal(st.loadSettings().animations,false);assert.ok(KEYS.history.includes("v5"));console.log("LEVEL 4: ALL TESTS PASSED");